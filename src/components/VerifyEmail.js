import React from 'react';

const VerifyEmail = () => {
  return (
    <div className='content-page signup-confirm'>
      <div className='content-wrapper'>
        <h2>Thank you for signing up!</h2>
        <div>Check your email to verify your account and get access to all the website's features.</div>
        <div>Note: you can always send another verification email by going in your user's settings.</div>
      </div>
    </div>
  );
}

export default VerifyEmail;
